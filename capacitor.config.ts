import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'loto-gagnant',
  appName: 'winning-number',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
